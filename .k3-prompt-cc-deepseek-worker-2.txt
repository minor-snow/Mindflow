You are implementing the RetryManager module for Mindflow Delivery Harness slice-001.

Working directory: /home/centi/Mindflow
Branch: k3/slice-001

Create the file src/retry-manager/index.ts with:
1. Export type RetryClassification = "transient" | "permanent" | "needs_human"
2. Export type AgentResult = { success: boolean; output: string; error?: string; retryable?: boolean; }
3. Export function classify(result: AgentResult): RetryClassification
   - If result.success is true, this should not be called (throw Error)
   - If result.retryable is true, return "transient"
   - If result.error contains "human" or "approval", return "needs_human"
   - Otherwise return "permanent"
4. Export function shouldRetry(classification: RetryClassification): boolean
   - Returns true only for "transient"

Keep it minimal. Only create src/retry-manager/index.ts. Do not modify any other files.